apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pv-claim-g43-1
  labels:
    app: postgres
spec:
  storageClassName: microk8s-hostpath
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
